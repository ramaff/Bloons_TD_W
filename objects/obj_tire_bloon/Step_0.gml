/// @description Insert description here
// You can write your code in this editor


// Inherit the parent event
event_inherited();

if added_speed > 0 {
	added_speed -= 0.15;
	speed -= 0.15;
	path_speed -= 0.15;
}